# Stackr P2 fixes — 8 September 2026

Implemented on the existing `codex/testflight-p1-20260908` release branch and draft PR #152. Of the 38 P2 findings in the earlier screen audit, 32 apply to this checkout and are implemented, one was already addressed, and five reference screens or controls absent from this release.

These are source changes for the next build. The currently available TestFlight version remains **1.0.3 (28)**, which contains the earlier P1 fixes. This P2 work has not been uploaded to TestFlight.

## Implemented findings

| Finding | Result |
| --- | --- |
| SHELL-001 | Main navigation exposes a tablist and exactly one selected destination on web. |
| SHELL-002 | Shared sheets have named dialogs, contained/restored web focus, non-focusable backdrops, keyboard avoidance, scrolling and safe-area padding. Search options expose pressed state. |
| SEARCH-001 | Compact type chooser, relevant facets, incompatible-filter reset, early language options and persistent Show results action. |
| EXPLORE-001 | Known series are resolved from catalogue identity; missing-series catalogues fall back to visible chronological groups. |
| EXPLORE-002 | Set search and its clear action have persistent accessible names. |
| HOME-001 | Home utility forms use the shared sheet; errors preserve drafts; failed HTTP responses do not clear submitted text. |
| COMM-002 | News distinguishes loading, failure and a genuinely empty feed, with retry and stale-request protection. |
| COMM-003 | Meetup fields and publishing remain in a bounded, keyboard-aware form with inline errors and duplicate-submit protection. |
| COMM-004 | Failed shop directions, website or phone opening reports an error while retaining the shop sheet. |
| COMM-005 | Community sheet close/clear controls have descriptive names and larger targets. |
| FRIENDS-001 | Both Friends entry actions open Find collectors directly, with a return to Friends. |
| PROFILE-002 | Collector names wrap without shrinking. |
| PROFILE-003 | Friend actions expose clear busy/disabled states and reject duplicate operations. Report/Block controls are absent from this release. |
| TXN-006 | Achievements switch to one column on small screens or enlarged native text, with wrapping descriptions. |
| ADM-001 | Recognition decisions require a scrollable card/decision confirmation; failures retain the review and appear inside the sheet. |
| ADM-002 | Public content has a relevant-field preview, public audience/status, cancellable confirmation and guarded publishing. |
| PRICE-001 | Search and item price failures remain visible with retry; unavailable estimates are not shown as zero. |
| PRICE-003 | Price watches show mutation progress and durable failure feedback. |
| PRODUCT-001 | Saved products are visible in Saved market items; unavailable product IDs remain identifiable and removable. |
| PRODUCT-002 | Product save/remove has busy/error recovery, owner isolation, serialized removals and stale-load protection. |
| PRODUCT-003 | Estimates use source price range wording and identify the available source; missing update time is explicitly unavailable. |
| TXN-003 | Draft restore failure blocks autosave; retry or guarded discard is available without silently replacing an unreadable draft. |
| BINDER-003 | Add-card identity names and set/number/language metadata wrap. |
| BINDER-004 | Edition choices remain visible and busy during a single guarded save; failure keeps the sheet open. |
| CARD-002 | A directly opened card returns to Search when there is no back history. |
| GRADE-001 | Denied native camera permission offers Settings and refreshes permission state on return. |
| SCAN-004 | Manual-search context no longer shrinks into one line. |
| SCAN-005 | Crop-upload failure has a persistent retry; local-retention wording reflects durable storage, and upload success requires confirmation. |
| SCAN-007 | Binder destinations have loading/error/retry states; saving requires a loaded, valid destination without discarding pocket decisions. |
| SCAN-008 | The legacy camera route redirects to the canonical scanner while preserving route parameters. |
| SET-002 | Quantity uses a labeled, keyboard-aware shared sheet with busy dismissal protection and 48-point controls. Existing quantity range remains 1–99. |
| SET-003 | Empty filtered sets provide a one-tap reset of search and facets. |

## Reconciled findings

| Finding | Current-release disposition |
| --- | --- |
| BINDER-002 | Already addressed: the primary binder detail title wraps to two lines. |
| PROFILE-001 | The audited hardcoded Report/Block controls are absent; no new reporting service was activated. |
| REVIEW-001 | The audited review screen is now a redirect; its old rating controls are absent. |
| TXN-002 | The audited seller Resubmit control is absent. |
| POKE-001 | The audited destructive Mark missing mutation is absent; current controls filter the catalogue. |
| SCAN-006 | The audited sweep-result route is absent. |

## Validation

- Full TypeScript check and production web export passed.
- Lint passed with zero errors and ten existing warnings.
- P1 and P2 regressions, Home release, binder catalogue, English set visibility, mobile scanner routing, recognition feedback, shared UX service and commerce release-lock checks passed.
- Source secret scan passed. P2 regression checks are included in Platform CI.
- Browser checks verified Search/Scan selected semantics; named Search filters; focus containment, Escape and focus return; Sets/Collectors facet scope; and a reachable 48-pixel results action within a 320 × 640 viewport.

Native iPhone/Android keyboard overlap, 200% text, VoiceOver/TalkBack, camera hardware and Settings permission return still need device validation. No live publishing, meetup, upload, ownership or saved-item mutations were performed during browser verification.
