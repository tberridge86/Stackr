// These approvals are intentionally controlled in source code, not deployment configuration.
// Turning either value on requires a reviewed code change as well as its environment flag.
export const commerceReleaseApprovals = Object.freeze({
  livePayments: false,
  liveShipping: false,
});
