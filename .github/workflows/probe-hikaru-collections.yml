name: Probe Hikaru collection logo catalogue

on:
  push:
    branches:
      - artifact/traditional-chinese-logos-a4-v2
    paths:
      - .github/scripts/probe_hikaru_collections.py
      - .github/workflows/probe-hikaru-collections.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install --disable-pip-version-check requests
      - run: python .github/scripts/probe_hikaru_collections.py
      - uses: actions/upload-artifact@v4
        with:
          name: hikaru-collections-probe
          path: build/hikaru-collections-probe/
          if-no-files-found: error
          retention-days: 3
