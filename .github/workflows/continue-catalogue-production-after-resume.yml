name: Continue StackR Catalogue Production After Resume

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain the replacement
        run: >-
          echo "Retired: normalize-resumed-catalogue-backfill-evidence.yml now independently validates the five language artifacts and dispatches the guarded production controller."
