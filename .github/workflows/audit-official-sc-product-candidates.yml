name: Audit official Simplified Chinese product candidates

on:
  push:
    branches:
      - artifacts/sc-set-image-retry-20260911
    paths:
      - artifact-jobs/simplified-chinese-set-logos/official_candidate_audit.py
      - .github/workflows/audit-official-sc-product-candidates.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - run: python -m pip install requests pillow reportlab beautifulsoup4
      - name: Download and lay out official image candidates
        working-directory: artifact-jobs/simplified-chinese-set-logos
        run: python official_candidate_audit.py
      - uses: actions/upload-artifact@v4
        with:
          name: official-simplified-chinese-product-candidates
          path: artifact-jobs/simplified-chinese-set-logos/official-candidate-audit/
          if-no-files-found: error
          retention-days: 14
          compression-level: 8
